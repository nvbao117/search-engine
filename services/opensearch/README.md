# OpenSearch notes

This MVP keeps analyzer config inside `scripts/ingest_index/index_articles.py` for one-command index bootstrap.

`services/opensearch/synonyms/news_synonyms.txt` is the human-edited source of truth for future synonym tuning.

